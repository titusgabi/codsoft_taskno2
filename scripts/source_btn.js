const source_code = "https://github.com/Debetome/quotesapp.github.io";
const source_btn = document.getElementById("source");

source_btn.addEventListener("click", () => {
    window.location = source_code;
});